import { ShirtSizePipe } from './shirt-size.pipe';

describe('ShirtSizePipe', () => {
  it('create an instance', () => {
    const pipe = new ShirtSizePipe();
    expect(pipe).toBeTruthy();
  });
});
